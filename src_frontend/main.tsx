import React from "react";
import ReactDOM from "react-dom/client";
import SendLetter from "./pages/SendLetterPage/SendLetter";

ReactDOM.createRoot(document.getElementById("root") as HTMLElement).render(
  <React.StrictMode>
    <SendLetter />
  </React.StrictMode>
);
